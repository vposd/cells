{
  "name": "cells",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve": "npx parcel serve ./src/index.html --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.2.0"
  }
}
